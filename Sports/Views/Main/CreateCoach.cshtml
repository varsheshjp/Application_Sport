@model Sports.Data.User
@{
}
<div class="container" style="margin:10px">
    <div>
        <form asp-action="AddUser" asp-controller="Main" method="post">
            <div class="form-group">
                <label>Enter Name</label>
                <input asp-for="Name" type="Text" name="Name" id="Name" class="form-input">
                <input type="hidden" name="Type" id="Type" asp-for="Type" value="Coach">
            </div>
            <button type="submit" class="btn btn-primary">Add</button>
            <a class="btn btn-primary" asp-action="Index" asp-controller="Main">cancel</a>
        </form>
    </div>
</div>